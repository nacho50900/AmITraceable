fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
httpx>=0.27,<1.0
python-dotenv>=1.0,<2.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
spacy>=3.7,<3.9
scikit-learn>=1.5,<1.7
numpy>=1.26,<3.0
# pandas: dependencia real de app/vision/geolocation.py (metadatos del
# índice FAISS, _index_meta, se manejan como DataFrame) y de
# scripts/build_faiss_index.py. No es solo para tests: en cuanto el índice
# de geolocalización esté construido, _lazy_load() la importa en producción.
pandas>=2.0,<3.0
# Pillow: dependencia real de app/vision/geolocation.py -- se usa para
# decodificar cada foto descargada en memoria en cuanto hay al menos una
# publicación candidata, incluso antes de comprobar si el índice FAISS
# existe.
#
# El resto de dependencias pesadas del módulo (torch, faiss, transformers)
# están en requirements-vision.txt, NO aquí -- pero ojo, a diferencia de lo
# que decía antes este comentario: no es que "solo hagan falta para
# construir el índice". También hacen falta EN EL ANÁLISIS (para calcular
# el embedding de cada foto del usuario y consultar el índice ya
# construido), así que si quieres que la geolocalización funcione de
# verdad en un backend desplegado con Docker, tienes que instalar también
# requirements-vision.txt en esa imagen -- ver el ARG WITH_GEOLOCATION del
# Dockerfile. El código se degrada solo (sin romper nada) si faltan, pero
# eso significa que la función simplemente no estará disponible, no que no
# hagan falta.
pillow>=10.0,<12.0
itsdangerous>=2.2,<3.0
emoji>=2.13,<3.0
python-jose[cryptography]>=3.3,<4.0
prometheus-fastapi-instrumentator>=7.0,<9.0
