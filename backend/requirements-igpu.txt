# MOVIDO a backend/igpu_worker/requirements.txt.
#
# torch-directml ya NO se instala en el backend en absoluto (ver el
# docstring de backend/igpu_worker/app.py para el motivo completo:
# torch-directml exige una versión de torch incompatible con el build
# CUDA/cu121 que este backend usa para Moondream2 -- instalarlo aquí
# reinstalaba torch y rompía ese CUDA, esto pasó de verdad en
# producción). El offload a iGPU vive ahora en un proceso/imagen Docker
# completamente aparte (backend/igpu_worker/), al que este backend habla
# por HTTP -- ver ENABLE_IGPU_OFFLOAD e IGPU_WORKER_URL en
# app/config.py.
#
# Este fichero se deja vacío a propósito, solo como puntero para quien
# busque por costumbre en la ruta antigua.
