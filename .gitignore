node_modules
coverage
lcov.info
docs/build
.env
webapp/.env
backend/.env
.vscode/

# Python
__pycache__/
*.pyc
.pytest_cache/
.coverage
coverage.xml
venv/
.venv/

# Datos y artefactos del módulo de geolocalización por imagen (regenerables,
# ver users/scripts/download_osv5m_spain.py y build_faiss_index.py -- no se
# versionan por tamaño, igual que no hay base de datos persistente en el
# resto del proyecto)
backend/data/
backend/scripts/_hf_scratch_cache/
backend/scripts/_completed_shards.txt
*.faiss

# Graphify: artefactos y configuración específicos de cada máquina.
# graphify-out/ (graph.json, graph.html, GRAPH_REPORT.md, manifest.json) SÍ se versiona
# -- ver GRAPHIFY_SETUP.md -- pero lo siguiente es local o depende de la ruta de instalación:
graphify-out/cost.json
graphify-out/cache/
.claude/settings.json

# Backups con fecha que graphify crea automáticamente al sobrescribir etiquetas curadas
graphify-out/20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]/

# Cache de los scripts de actualización de datos estadísticos
backend/scripts/.cache_studies/